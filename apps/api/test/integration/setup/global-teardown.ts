/**
 * Global teardown for integration tests.
 * Runs once after all test files complete.
 */
export default async function globalTeardown(): Promise<void> {
  // No-op for now; add cleanup (e.g. close global DB pool) if needed
}
